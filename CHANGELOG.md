# Changelog

## 2026-04-09

### 巡检分页与自动禁用统计修复

- 修复 `app/core/automation.py` 中渠道列表只读取单页的问题，改为从第 1 页开始逐页抓取并按渠道 ID 去重。
- 修复后重新执行巡检，确认自动禁用渠道统计从 `3` 修正为 `14`。
- 补充 `tests/test_core_logic.py` 回归测试，覆盖分页抓取与去重行为，避免后续再次退化为只读第一页。

### 手动任务执行与结果展示

- 修复“执行任务”页签下手动触发任务后，当前结果未切换到最新 `run_id` 的问题。
- 保持任务执行后自动刷新，并优先展示本次刚执行完成的任务结果。

### 前端展示优化

- 在总览和执行任务区域新增渠道统计卡片，展示自动禁用总数、实际参与任务数、优先级过滤数、Codex 渠道数、非 Codex 渠道数。
- 移除总览区域重复的旧统计块，只保留新的渠道统计卡片。
- 优化“执行任务”区域 `summary-header` 展示样式，改为卡片化元信息布局。
- 调整 `jobChannelStats` 与下方结果区块间距，避免内容贴得过近。

### 验证记录

- 执行巡检任务并确认最新记录：
  `run_id = 148ff4aebc044b3f9da9874465011915`
- 测试通过：
  `13 passed`
